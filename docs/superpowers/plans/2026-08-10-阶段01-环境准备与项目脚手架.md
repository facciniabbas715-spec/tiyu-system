# 阶段 0-1：环境准备与项目脚手架 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立前后端可编译、可启动的项目骨架（`sportseq-admin` 后端 + `sportseq-web` 前端），为阶段 2 数据库与基础设施做准备。

**Architecture:** 后端使用 Spring Initializr 生成 Maven 工程（自带 mvnw，规避本机未装 Maven 的问题），保留 web/validation/lombok 最小依赖，提供 `/api/health` 健康检查与统一响应 `Result<T>`；前端使用 Vite 的 vue-ts 模板，集成 Element Plus、Pinia、Vue Router、Axios，搭好目录与请求封装骨架。**本阶段不引入 Redis、数据库、MyBatis-Plus 与安全框架。**

**Tech Stack:** JDK 24 + Spring Boot 3.5.x + Maven Wrapper；Node 24 + Vite + Vue3 + TS + Element Plus + Pinia + Vue Router + Axios。

---

## 环境前提（本机已核实）

- JDK 24.0.1 已安装（`java -version` 可用）
- Node 24.15.0 / npm 11.12.1 已安装
- MySQL 8.0.45 运行中（本阶段不连接）
- Redis 本阶段不需要（用户明确暂不开发 Redis 服务）
- 端口 8080 空闲；工作目录：`E:\codex——vibecoding works\体育器材管理系统`

## 任务总览

| Task | 内容 | 验证方式 | 产出 |
| --- | --- | --- | --- |
| 1 | 生成后端工程骨架 | `mvnw -v` 可用、目录存在 | `sportseq-admin/` |
| 2 | 统一 Result + 健康检查接口 | 单元测试通过 | `Result`、`HealthController` |
| 3 | 后端启动验证 | curl 返回 OK | 可运行的后端 |
| 4 | 生成前端工程并装依赖 | `npm install` 成功 | `sportseq-web/` |
| 5 | 前端骨架代码 | `npm run type-check` 通过 | 配置、封装、骨架页面 |
| 6 | 前端构建与启动验证 | build 成功、dev 可达 | 可运行的前端 |
| 7 | 提交 commit 2 | `git log` 可见 | 里程碑提交 |

---

## Task 1: 生成后端工程骨架

**Files:**
- Create: `sportseq-admin/`（Initializr 生成）

- [ ] **Step 1.1: 通过 Spring Initializr 生成工程**

在仓库根目录（`E:\codex——vibecoding works\体育器材管理系统`）执行：

```powershell
Invoke-WebRequest "https://start.spring.io/starter.zip" -Method Post -Body @{
  type = "maven-project"
  language = "java"
  bootVersion = "3.5.7"
  javaVersion = "17"
  groupId = "com.company"
  artifactId = "sportseq-admin"
  name = "sportseq-admin"
  packageName = "com.company.sportseq"
  dependencies = "web,validation,lombok"
} -OutFile "$env:TEMP\sportseq-admin.zip"
Expand-Archive -LiteralPath "$env:TEMP\sportseq-admin.zip" -DestinationPath "." -Force
Remove-Item -LiteralPath "$env:TEMP\sportseq-admin.zip" -Force
```

Expected: 生成 `sportseq-admin/` 目录，含 `pom.xml`、`mvnw.cmd`、`src/main/java/com/company/sportseq/SportseqAdminApplication.java`、`src/test/java/...`。

> 若 `bootVersion=3.5.7` 报 404：先执行 `Invoke-RestMethod "https://start.spring.io/metadata/client" | ConvertTo-Json -Depth 5 | Select-String '3\.5\.'` 获取最新 3.5.x 版本号，替换后重试。

- [ ] **Step 1.2: 确认 Maven Wrapper 可执行**

```powershell
cd sportseq-admin; .\mvnw.cmd -v
```

Expected: 输出 Maven 版本号（首次运行会联网下载 Maven 分发包）。

- [ ] **Step 1.3: 确认初始测试可运行（脚手架基线）**

```powershell
.\mvnw.cmd test
```

Expected: `BUILD SUCCESS`，`Tests run: 1`（默认 `contextLoads`）。

- [ ] **Step 1.4: Commit（可选，若 1-4 一起提交则跳过）**

```powershell
cd ..
git add sportseq-admin
git commit -m "chore: 引入后端 Spring Boot 工程骨架"
```

## Task 2: 统一 Result 与健康检查接口

**Files:**
- Create: `sportseq-admin/src/main/java/com/company/sportseq/common/result/Result.java`
- Create: `sportseq-admin/src/main/java/com/company/sportseq/controller/HealthController.java`
- Create: `sportseq-admin/src/test/java/com/company/sportseq/controller/HealthControllerTest.java`
- Modify: `sportseq-admin/src/main/resources/application.yml`

- [ ] **Step 2.1: 编写失败的测试（TDD）**

`sportseq-admin/src/test/java/com/company/sportseq/controller/HealthControllerTest.java`：

```java
package com.company.sportseq.controller;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.test.web.servlet.MockMvc;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@WebMvcTest(HealthController.class)
class HealthControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    void health_shouldReturnOk() throws Exception {
        mockMvc.perform(get("/api/health"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.code").value(0))
                .andExpect(jsonPath("$.data").value("OK"));
    }
}
```

- [ ] **Step 2.2: 运行测试，确认失败（HealthController/Result 不存在，编译失败）**

```powershell
.\mvnw.cmd test
```

Expected: `BUILD FAILURE`（找不到 `HealthController` 类）。

- [ ] **Step 2.3: 创建统一响应 Result**

`sportseq-admin/src/main/java/com/company/sportseq/common/result/Result.java`：

```java
package com.company.sportseq.common.result;

import java.io.Serializable;

/**
 * 统一响应结构：code=0 表示成功；非 0 为业务/系统错误码。
 */
public record Result<T>(int code, String message, T data, String traceId) implements Serializable {

    public static <T> Result<T> success(T data) {
        return new Result<>(0, "success", data, null);
    }

    public static <T> Result<T> success() {
        return new Result<>(0, "success", null, null);
    }

    public static <T> Result<T> error(int code, String message) {
        return new Result<>(code, message, null, null);
    }
}
```

- [ ] **Step 2.4: 创建健康检查接口**

`sportseq-admin/src/main/java/com/company/sportseq/controller/HealthController.java`：

```java
package com.company.sportseq.controller;

import com.company.sportseq.common.result.Result;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api")
public class HealthController {

    @GetMapping("/health")
    public Result<String> health() {
        return Result.success("OK");
    }
}
```

- [ ] **Step 2.5: 配置 application.yml**

将 `sportseq-admin/src/main/resources/application.yml` 整体替换为：

```yaml
server:
  port: 8080

spring:
  application:
    name: sportseq-admin
  profiles:
    active: dev
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss

# 阶段 2 引入：数据源、MyBatis-Plus、Druid、Flyway
# 阶段 3 引入：Redis、Spring Security
```

- [ ] **Step 2.6: 运行测试，确认通过**

```powershell
.\mvnw.cmd test
```

Expected: `BUILD SUCCESS`，`Tests run: 2`（contextLoads + health_shouldReturnOk）。

## Task 3: 后端启动验证

**Files:** 无新增

- [ ] **Step 3.1: 启动后端**

```powershell
Start-Process -FilePath ".\mvnw.cmd" -ArgumentList "spring-boot:run" -WindowStyle Hidden
```

等待 30-60 秒（首次下载依赖较慢），然后：

```powershell
Start-Sleep -Seconds 45
Invoke-RestMethod "http://localhost:8080/api/health"
```

Expected: 返回 `code=0, data="OK"`。

- [ ] **Step 3.2: 停止后端（验证完成后）**

```powershell
Get-Process | Where-Object { $_.ProcessName -match 'java' } | Stop-Process -Force
```

> 注意：此命令会终止所有 java 进程；若本机有其他 Java 服务请改为仅终止 `sportseq-admin` 对应进程（用 `Get-CimInstance Win32_Process | Where-Object CommandLine -match 'sportseq-admin'` 先确认 PID）。

## Task 4: 生成前端工程并安装依赖

**Files:**
- Create: `sportseq-web/`（Vite 生成）

- [ ] **Step 4.1: 使用 Vite 创建 vue-ts 工程**

```powershell
cd ..
npm create vite@latest sportseq-web -- --template vue-ts
```

Expected: 生成 `sportseq-web/`，含 `package.json`、`vite.config.ts`、`tsconfig*.json`、`src/`。

- [ ] **Step 4.2: 安装运行时与开发依赖**

```powershell
cd sportseq-web
npm install
npm install element-plus @element-plus/icons-vue vue-router pinia axios echarts dayjs nprogress
npm install -D sass @types/nprogress
```

Expected: `npm install` 全部成功，无红色错误。

- [ ] **Step 4.3: 清理模板演示代码**

删除 `src/components/HelloWorld.vue`、`src/assets/vue.svg`、`src/style.css` 与 `public/vite.svg`（模板演示文件），并将 `index.html` 的 `<title>` 改为 `体育器材管理系统`。

## Task 5: 前端骨架代码

**Files:**
- Modify: `sportseq-web/vite.config.ts`
- Modify: `sportseq-web/tsconfig.app.json`
- Create: `sportseq-web/.env.development`、`sportseq-web/.env.production`
- Create: `sportseq-web/src/main.ts`
- Create: `sportseq-web/src/App.vue`
- Create: `sportseq-web/src/styles/index.scss`
- Create: `sportseq-web/src/router/index.ts`
- Create: `sportseq-web/src/store/index.ts`
- Create: `sportseq-web/src/api/request.ts`
- Create: `sportseq-web/src/utils/auth.ts`
- Create: `sportseq-web/src/views/login/index.vue`
- Create: `sportseq-web/src/views/error/404.vue`

- [ ] **Step 5.1: 安装 Node 类型（vite.config 需要）**

```powershell
npm install -D @types/node
```

- [ ] **Step 5.2: 配置 vite.config.ts（别名 + API 代理）**

将 `sportseq-web/vite.config.ts` 整体替换为：

```ts
import { fileURLToPath, URL } from 'node:url'
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
})
```

- [ ] **Step 5.3: 配置 tsconfig.app.json（支持 @ 别名）**

在 `sportseq-web/tsconfig.app.json` 的 `compilerOptions` 中新增（保留原有其他配置）：

```json
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
```

- [ ] **Step 5.4: 创建环境变量文件**

`sportseq-web/.env.development`：

```text
VITE_APP_TITLE=体育器材管理系统
VITE_API_BASE_URL=/api
```

`sportseq-web/.env.production` 内容相同。

- [ ] **Step 5.5: 重写 main.ts**

`sportseq-web/src/main.ts` 整体替换为：

```ts
import { createApp } from 'vue'
import ElementPlus from 'element-plus'
import zhCn from 'element-plus/es/locale/lang/zh-cn'
import 'element-plus/dist/index.css'
import App from './App.vue'
import router from './router'
import store from './store'
import '@/styles/index.scss'

const app = createApp(App)

app.use(store)
app.use(router)
app.use(ElementPlus, { locale: zhCn })

app.mount('#app')
```

- [ ] **Step 5.6: 重写 App.vue**

`sportseq-web/src/App.vue` 整体替换为：

```vue
<template>
  <router-view />
</template>

<script setup lang="ts"></script>
```

- [ ] **Step 5.7: 创建全局样式**

`sportseq-web/src/styles/index.scss`：

```scss
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html,
body,
#app {
  height: 100%;
}

body {
  font-family: 'Helvetica Neue', Helvetica, 'PingFang SC', 'Microsoft YaHei', Arial, sans-serif;
  font-size: 14px;
  color: #303133;
}
```

- [ ] **Step 5.8: 创建 Pinia store 入口**

`sportseq-web/src/store/index.ts`：

```ts
import { createPinia } from 'pinia'

const store = createPinia()

export default store
```

- [ ] **Step 5.9: 创建路由**

`sportseq-web/src/router/index.ts`：

```ts
import { createRouter, createWebHistory, type RouteRecordRaw } from 'vue-router'

const routes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/login/index.vue'),
    meta: { title: '登录' },
  },
  { path: '/', redirect: '/login' },
  {
    path: '/:pathMatch(.*)*',
    name: 'NotFound',
    component: () => import('@/views/error/404.vue'),
    meta: { title: '404' },
  },
]

const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes,
})

export default router
```

- [ ] **Step 5.10: 创建 token 存取工具**

`sportseq-web/src/utils/auth.ts`：

```ts
const TOKEN_KEY = 'sportseq_token'

export function getToken(): string | null {
  return localStorage.getItem(TOKEN_KEY)
}

export function setToken(token: string): void {
  localStorage.setItem(TOKEN_KEY, token)
}

export function removeToken(): void {
  localStorage.removeItem(TOKEN_KEY)
}
```

- [ ] **Step 5.11: 创建 Axios 请求封装**

`sportseq-web/src/api/request.ts`：

```ts
import axios, { type AxiosRequestConfig } from 'axios'
import { ElMessage } from 'element-plus'
import { getToken, removeToken } from '@/utils/auth'

export interface ApiResult<T = unknown> {
  code: number
  message: string
  data: T
  traceId: string | null
}

const request = axios.create({
  baseURL: '/api',
  timeout: 15000,
})

request.interceptors.request.use((config) => {
  const token = getToken()
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

request.interceptors.response.use(
  (response) => {
    const res = response.data as ApiResult
    if (res.code !== 0) {
      ElMessage.error(res.message || '请求失败')
      if (res.code === 401) {
        removeToken()
        window.location.href = '/login'
      }
      return Promise.reject(new Error(res.message))
    }
    return response
  },
  (error) => {
    ElMessage.error(error.response?.data?.message || '网络异常，请稍后重试')
    return Promise.reject(error)
  },
)

export function get<T = unknown>(url: string, config?: AxiosRequestConfig): Promise<T> {
  return request.get(url, config).then((res) => (res.data as ApiResult<T>).data)
}

export function post<T = unknown>(url: string, data?: unknown, config?: AxiosRequestConfig): Promise<T> {
  return request.post(url, data, config).then((res) => (res.data as ApiResult<T>).data)
}

export default request
```

- [ ] **Step 5.12: 创建登录页骨架**

`sportseq-web/src/views/login/index.vue`：

```vue
<template>
  <div class="login-page">
    <h1>体育器材管理系统</h1>
    <p>登录功能将在认证模块（阶段 3）实现</p>
  </div>
</template>

<script setup lang="ts"></script>

<style scoped lang="scss">
.login-page {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
}
</style>
```

- [ ] **Step 5.13: 创建 404 页**

`sportseq-web/src/views/error/404.vue`：

```vue
<template>
  <div class="not-found">
    <h1>404</h1>
    <p>页面不存在</p>
    <el-button type="primary" @click="$router.push('/')">返回首页</el-button>
  </div>
</template>

<script setup lang="ts"></script>

<style scoped lang="scss">
.not-found {
  text-align: center;
  padding-top: 120px;
}
</style>
```

## Task 6: 前端构建与启动验证

**Files:** 无新增

- [ ] **Step 6.1: 类型检查**

```powershell
npm run type-check
```

Expected: 无 TypeScript 错误。

- [ ] **Step 6.2: 生产构建**

```powershell
npm run build
```

Expected: `dist/` 生成，构建成功。

- [ ] **Step 6.3: 启动 dev server 并验证可达**

```powershell
Start-Process -FilePath "npm" -ArgumentList "run", "dev" -WindowStyle Hidden
Start-Sleep -Seconds 10
Invoke-WebRequest "http://localhost:5173" -UseBasicParsing
```

Expected: 返回 200；浏览器访问 `http://localhost:5173/login` 可见登录页骨架。

- [ ] **Step 6.4: 停止 dev server（验证完成后）**

```powershell
Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match 'vite' } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

## Task 7: 提交 commit 2

**Files:** 全部脚手架变更

- [ ] **Step 7.1: 提交前后端脚手架**

```powershell
cd ..
git add sportseq-admin sportseq-web .gitignore README.md docs
git commit -m "feat: 前后端项目脚手架（commit 2）"
```

- [ ] **Step 7.2: 确认提交历史**

```powershell
git log --oneline
```

Expected: 两个提交——commit 1（设计文档入库）与 commit 2（脚手架）。

---

## 本阶段验收清单（全部通过方可进入阶段 2）

- [ ] `sportseq-admin` 通过 `mvnw test`，`/api/health` 返回 `Result` 结构
- [ ] `sportseq-web` 通过 `type-check` 与 `build`，dev server 可访问
- [ ] 前后端目录结构与设计文档 §4.5 / §5.2 一致（本阶段先落骨架，后续阶段按设计补齐）
- [ ] commit 2 已提交，`git status` 干净
- [ ] 本阶段未引入 Redis、数据库连接、安全框架（遵循用户"暂不开发 Redis 服务"的要求）
