# 阶段 2：数据库与基础设施 实施计划

> **For agentic workers:** 本阶段由主代理（/root）内联执行，按 executing-plans 流程分批实施、检查点验证。步骤使用 checkbox（`- [ ]`）追踪。

**Goal:** 落地 MySQL 数据库（Flyway 管理 24 张表与种子数据），完成后端基础设施（统一响应/错误码/全局异常、MyBatis-Plus、Druid、Knife4j、CORS），为认证与业务模块打底。

**Architecture:** Spring Boot 3.5.16 + MyBatis-Plus 3.5.9（spring-boot3 starter）+ Druid 连接池 + Flyway 版本化 SQL；统一 `Result<T>`/`PageResult<T>`、分级错误码、`BizException` + 全局异常处理器；Knife4j(OpenAPI3) 接口文档；CORS 允许开发跨域。

**Tech Stack:** MySQL 8.0（库 `sportseq`，账号 `sportseq`）、Flyway 9/10（随 Spring Boot 管理）、Knife4j 4.5.x、Hutool 5.8.x。

---

## 环境前提（已核实）

- MySQL 8.0.45 运行中（127.0.0.1:3306），root 密码已提供；已创建 `sportseq` 库与账号 `sportseq`/`Sportseq@123`
- JDK 24 + Maven Wrapper；后端工程位于 worktree：`E:\codex——vibecoding works\体育器材管理系统\.worktrees\phase-01-scaffold\sportseq-admin`

## 任务分解

| Task | 内容 | 验证方式 | 产出 |
| --- | --- | --- | --- |
| 2.1 | pom 添加依赖 | `mvnw dependency:resolve` 成功 | 更新后的 pom.xml |
| 2.2 | 数据源与框架配置 | 启动连接数据库成功 | application.yml / application-dev.yml、MybatisPlusConfig |
| 2.3 | 公共基础设施代码 | 单元测试通过 | ErrorCode、BizException、GlobalExceptionHandler、PageResult、BaseEntity |
| 2.4 | 接口文档与跨域 | /v3/api-docs 可访问 | Knife4jConfig、CorsConfig |
| 2.5 | Flyway V1：24 张表 DDL | 迁移执行无错误 | `V1__schema.sql` |
| 2.6 | Flyway V2：字典/管理员/菜单种子 | 迁移后数据可查询 | `V2__seed.sql` |
| 2.7 | 全量验证 | 测试全绿 + 启动 + 接口可用 | 可运行后端 |
| 2.8 | 提交 commit 3 / commit 4 | git log 可见 | 数据库落地 + 基础设施两个里程碑 |

## 关键验证命令

```powershell
cd sportseq-admin
.\mvnw.cmd test
.\mvnw.cmd spring-boot:run
Invoke-RestMethod http://localhost:8080/api/health
Invoke-RestMethod http://localhost:8080/v3/api-docs
```

## 验收标准

- [ ] Flyway 两个版本迁移成功（`flyway_schema_history` 记录 2 行）
- [ ] 24 张表全部创建，字段/索引与设计文档一致
- [ ] 统一异常：`BizException` 返回对应错误码；参数校验失败返回 1001；未知异常返回 5000
- [ ] MyBatis-Plus 分页/逻辑删除/乐观锁/自动填充配置生效
- [ ] 健康检查与 Knife4j 文档可访问
- [ ] commit 3（Flyway 脚本）与 commit 4（基础设施）分别提交
